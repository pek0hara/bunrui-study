{
  "name": "bunrui-study",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute bunrui-study-db --local --file=./d1_schema.sql",
    "db:migrate:remote": "wrangler d1 execute bunrui-study-db --file=./d1_schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.0.0"
  }
}
